# Core data processing
pandas>=2.0.0
duckdb>=0.9.0

# PDF parsing
pdfplumber>=0.10.0

# HTTP requests (corpus download, OpenAlex API)
requests>=2.31.0

# YAML config parsing
pyyaml>=6.0

# Evaluation / budget ablation
rank-bm25>=0.2.2

# Optional: local semantic reranking (uncomment if running Level 2 budget ablation)
# sentence-transformers>=2.2.2
